Questa cartella può contenere eventuali librerie esterne (.jar) utilizzate per la compilazione e l'esecuzione dell'applicazione.
Se tutte le dipendenze (come il driver JDBC di PostgreSQL) sono gestite da Maven tramite il file pom.xml, questa cartella può rimanere vuota (o contenere solo questo file segnaposto).
