Inserisci in questa cartella i file eseguibili compilati (.jar) dell'applicazione:
1. Il JAR del Server (es. ServerTK.jar)
2. Il JAR del Client (es. ClientTK.jar)

Puoi generarli tramite IntelliJ o configurando opportunamente il packaging di Maven.
